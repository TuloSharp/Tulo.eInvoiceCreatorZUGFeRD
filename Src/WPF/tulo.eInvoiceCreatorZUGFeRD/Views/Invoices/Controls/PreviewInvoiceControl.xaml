<UserControl x:Class="Tulo.eInvoiceCreatorZUGFeRD.Views.Invoices.Controls.PreviewInvoiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Tulo.eInvoiceCreatorZUGFeRD.Views.Invoices.Controls"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:custombehaviors="clr-namespace:tulo.ResourcesWpfLib.Utilities;assembly=tulo.ResourcesWpfLib"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1"
              Background="White"
              Height="36">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Name="RefreshPreviewButton"
                    Style="{StaticResource ToolbarPrimaryButton}"
                    ToolTip="{Binding ToolTipRefreshPreview}"
                    Grid.Column="1"
                    Command="{Binding UpdatePreviewInvoicePdfCommand}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="{StaticResource RefreshIconSVG}"
                          Fill="White"
                          Width="14"
                          Height="14"
                          Stretch="Uniform"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0" />
                    <AccessText Text="{Binding ContentButtonRefresh}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

        </Grid>

        <wv2:WebView2 Name="webView" Grid.Row="3">
            <behaviors:Interaction.Behaviors>
                <custombehaviors:WebView2ViewerBehavior HtmlContent="{Binding DocumentSource}" />
            </behaviors:Interaction.Behaviors>
        </wv2:WebView2>
    </Grid>

</UserControl>